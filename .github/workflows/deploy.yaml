name: Build Vue

on:
  pull_request:
    branches: [ "master" ]

jobs:
  build_vue:
    runs-on: ubuntu-latest
    name: Build Vue
    steps:
    - uses: actions/checkout@v2
    - id: Build-Vue
      uses: xRealNeon/VuePagesAction@1.0.1
      with:
        username: AnastasiaIU
        reponame: bank-api-vue
        token: ${{ secrets.GITHUB_TOKEN }}
